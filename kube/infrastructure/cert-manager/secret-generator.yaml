apiVersion: viaduct.ai/v1
kind: ksops

metadata:
  name: cert-manager-secrets-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops

files:
  - secret.enc.yaml
