apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: prod
  labels:
    app: minio
    app.kubernetes.io/part-of: microphoto
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
