apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: prod
  labels:
    app: redis
    app.kubernetes.io/part-of: microphoto
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
