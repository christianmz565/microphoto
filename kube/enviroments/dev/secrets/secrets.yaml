apiVersion: v1
kind: Secret
metadata:
  name: microphoto-secrets
  namespace: dev
  labels:
    app.kubernetes.io/part-of: microphoto
type: Opaque
stringData:
  REDIS_URL: "redis:6379"
  MINIO_ENDPOINT: "minio:9000"
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
  MINIO_SSL: "false"
